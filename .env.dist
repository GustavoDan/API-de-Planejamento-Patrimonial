# Este é um arquivo de exemplo. Copie este arquivo para .env e preencha os valores.

# Conexão com o Banco de Dados
DATABASE_URL="postgresql://planner:plannerpw@db:5432/plannerdb?schema=public"

# Chave secreta para assinar os tokens JWT.
# Em produção, use uma string longa e aleatória (gerada por um gerador de senhas, por exemplo).
JWT_SECRET="super-secret-key-for-development-only"

# Credenciais para o primeiro usuário ADVISOR que será criado pelo script de seed (npx prisma db seed).
# Estes valores são usados apenas uma vez para popular o banco de dados.
SUPER_USER_EMAIL="advisor@example.com"
SUPER_USER_PASSWORD="strongpassword123"
